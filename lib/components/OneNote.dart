import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

import '../globals.dart';

Widget noteCard(int index, Color randColor) => Container(
      decoration: BoxDecoration(
        color: randColor,
        borderRadius: BorderRadius.circular(16.0),
      ),
      child: Column(
        children: [],
      ),
    );
